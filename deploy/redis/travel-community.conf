# Include these settings in /etc/redis/redis.conf.
bind 127.0.0.1 ::1
protected-mode yes
port 6379
supervised systemd

# Realtime state is temporary; MongoDB remains the durable source of truth.
save ""
appendonly no
maxmemory 256mb
maxmemory-policy allkeys-lru
